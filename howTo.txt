- install dependencies

    npm watch


- open chrome go to this url chrome://extension
- check the developer mode checkbox
- click upload unpacked extension
- go into the folder and select the dist folder only.

if you dont get any errors it should be loaded in nicely and easy.

- go to src/popup.... interface lives here.
- write the styles in dist/style.css directly
- lol lets right some modular css so I can play around with it too lol
- click reload to see changes after applying

COMPONENT HIERARCHY
App
  -Header
    -filter(search bar up there)
  -stack
    - tabItem(where tabs are added)
  -settings
    - settings button
    - tab count component



tabItem

    - Link Title - [webTitle]
    - Link = [webLink]
    - Remove Button - [removeBtn]
    - Icon .icon(wrapper)
    - Setting Btn [settingBtn]
    - Limit Btn [limitBtn]
    - Limit Counter [limitCounter]


     type: 'basic',
            iconUrl: './icons/icon48.png',
            title: 'Tab Limit has been reach',
            message: 'New Tab is due to be closed',
            buttons: [{ title: 'Yes' }, { title: 'No' }]

   chrome.notifications.create(`${tabInfo.id}`, {
        type: 'basic',
        iconUrl: './icons/icon48.png',
        title: 'Tab Limit has been reach',
        message: 'New Tab is due to be closed',
        buttons: [{ title: 'Yes' }, { title: 'No' }]
      });
